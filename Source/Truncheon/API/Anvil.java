package Truncheon.API;

import java.io.File;
//Scripting featureset

// COMMON COMPONENTS LIKE WAIT, INPUT, ECHO ARE HERE. NO REDUNDANCY
//ANY EXTRA FEATURES ARE PASSED BACK TO THE PROGRAM AND ARE PROCESSED BY THE CALLING PROGRAM
public class Anvil
{
    private String commonCommands(String command)
    {
        String result = "";
        //split the string into array
        //load the array into the switch statement
        switch()
        {
            default:
                //set the input string into the result
            break;
        }
        return result;
    }

    private void loadScriptFile(String fileName)
    {
        new File(fileName).exists();
    }
}
